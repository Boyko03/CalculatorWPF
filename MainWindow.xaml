<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        xmlns:viewModel="clr-namespace:Calculator.ViewModel"
        xmlns:controls="clr-namespace:Calculator.Controls"
        mc:Ignorable="d"
        Title="Calculator" Height="540" Width="335">
    <Grid Background="#232323">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <controls:MenuBar Margin="5 15 5 0"/>

        <controls:ResultBar Grid.Row="1" Margin="5 10 10 0" Foreground="PaleGreen"/>

        <controls:MemoryBar Grid.Row="2" Height="30" Margin="3 10 3 0"/>

        <controls:StandardInputBar Grid.Row="3" Margin="3"/>
    </Grid>
</Window>
