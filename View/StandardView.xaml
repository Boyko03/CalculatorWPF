<UserControl x:Class="Calculator.View.StandardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Calculator.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <controls:ResultBar Grid.Row="0" Margin="5 10 10 0" Foreground="PaleGreen"/>

        <controls:MemoryBar Grid.Row="1" Height="30" Margin="3 10 3 0"/>

        <controls:StandardInputBar Grid.Row="2" Margin="3"/>
    </Grid>
</UserControl>
